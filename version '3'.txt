version: '3'

services:

  db:
    container_name: notejam-db
    image: mysql
    restart: "no"
    environment:
      MYSQL_ROOT_PASSWORD: 1234qwer
    ports:
      - 3306
    volumes:
      - "./sql:/docker-entrypoint-initdb.d"

  app:
    container_name: notejam-app
    restart: "no"
    image: notejam-sqlapp
    depends_on:
      - "db"
    environment:
      MYSQL_HOST: "notejam-db"
      MYSQL_ROOT_PASSWORD: 1234qwer
      MYSQL_TCP_PORT: 3306
      NODE_ENV: development
      NOTEJAM_HOST: "notejam-app"
      NOTEJAM_PORT: 3000
    ports:
      - 3000:3000

volumes:
  sql:
  notejam:
















version: '3'

services:
  app:
    build:
      # context: /notejam-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - db
    environment:
      DB_HOST: localhost
      DB_USER: subhojyoti
      DB_PASSWORD: Singha@12
      DB_DATABASE: notejam_dev

  db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: Singha@12
      MYSQL_DATABASE: notejam_dev
